# bmi-기능명세서.md

## 1. 개요
비만도(BMI) 계산 메뉴의 기능 명세서입니다.

## 2. 화면 위치
1. 경로 : `/calculator/health/bmi`
2. 동적 경로 (SEO) : `/calculator/health/bmi/[slug]` (종목별 전용 페이지)
3. 파일
ㄴ 서버 : `app/calculator/health/bmi/page.tsx`
ㄴ 동적 서버 : `app/calculator/health/bmi/[slug]/page.tsx` (상태별 슬러그: 정상, 과체중, 비만 등)
ㄴ 클라이언트 : `app/calculator/health/bmi/Bmi.tsx`

## 3. 계층 구조 및 SEO (Common Architecture)
1. **Breadcrumb Schema**: `홈 > 계산기 홈 > 건강 계산기 > 비만도(BMI) 계산기` 구조의 JSON-LD 적용
2. **동적 메타데이터**: `[slug]`(성별, 나이, 키, 체중)에 따라 Title, Description, Keywords 자동 생성
3. **PWA 연동**: 
   - `PWAInstallProvider`를 통한 설치 가능 상태 감지
   - `InstallBanner` (페이지 하단), `Footer` (앱 설치 버튼) 연동
4. 뒤로가기 버튼 / 공유 버튼 (NavBar 컴포넌트 필수 사용)
5. JIKO 플랫폼 홈(`jiko.kr`) 허브 이동 버튼 (Header 및 Footer 영역에 단축 위치)

## 4. 화면 기능
1. 입력 데이터
ㄴ 성별
ㄴ 나이
ㄴ 키(cm)
ㄴ 체중(kg)
ㄴ 선택 입력 항목 (상세 분석)
   - 허리둘레(cm) / 엉덩이둘레(cm) : 복부 비만도(WHR) 계산용
   - 평소 활동량 (매우 낮음 ~ 매우 높음)

2. 출력 데이터
ㄴ 비만도(BMI)
ㄴ 체중 범위 (저체중, 정상, 과체중, 비만 - WHO/아시아태평양 기준)
ㄴ 복부 비만도(WHR) 및 위험도 (선택 입력 시)
ㄴ 나이 대비 BMI 분포 정보 (통계 기반 비교)
ㄴ 결과에 따른 맞춤형 몸관리 가이드 (식단 및 운동 제안)
ㄴ **시각화 요소**: BMI/체형에 따른 아바타 변화 (복근, 체형 실루엣 등)
ㄴ 결과 복사 기능 (텍스트 클립보드 복사)

3. 버튼 기능
ㄴ 초기화 : 모든 입력 데이터 삭제
ㄴ 계산하기 : 계산 결과 출력
ㄴ 결과 복사 : 결과 결과 복사

4. UI/UX 기능
ㄴ 초기화 버튼 클릭 시 흔들림 애니메이션 (shake) 효과 적용

5. 하단 정보 영역 (SEO 및 사용자 가이드 - page.tsx)
ㄴ 검색 엔진 최적화(SEO)를 위한 JSON-LD 라우팅 보강
ㄴㄴ `SoftwareApplication` 및 `FAQPage` 외 추가 범용 `schema` 스크립트 작성 및 로드
ㄴㄴ 구글 서치 콘솔(GSC) 등록 완료 (루트 경로 HTML 소유권 확인)
ㄴㄴ `sitemap.ts` 및 `robots.ts`를 통한 동적 크롤링 및 인덱싱 허용 (검색 노출 최적화)
ㄴ UI 디자인 : 본체와 동일한 카드형 레이아웃 및 다크모드 대응 적용
ㄴ 구성요소
ㄴㄴ 소개 : 비만도(BMI) 및 복부 비만도(WHR) 계산기 목적 안내
ㄴㄴ 사용 방법 : 성별, 나이, 키, 체중, (선택)허리둘레를 통한 비만 지표 계산 가이드
ㄴㄴ 계산 예시 : 30세 남성, 175cm, 70kg 기준 BMI 22.8(정상) 도출 예제
ㄴㄴ FAQ : BMI 공식(체중(kg) / (키(m)²)) 및 WHO/아시아태평양 비만 기준 안내 (**공통 `FAQ` 컴포넌트 적용**)
ㄴㄴ 건강 계산기 더 보기 : 다른 건강 계산기 메뉴 바로가기 (**공통 `HealthMoreCalculators` 컴포넌트 적용**)

## 5. 계산 로직
1. 비만도(BMI) = 체중(kg) / (키(m) × 키(m))
2. 복부 비만도(WHR) = 허리둘레(cm) / 엉덩이둘레(cm)
3. BMI 기준 (WHO 아시아-태평양):
   - 18.5 미만: 저체중
   - 18.5 ~ 22.9: 정상
   - 23.0 ~ 24.9: 과체중 (위험체중)
   - 25.0 ~ 29.9: 비만 (1단계)
   - 30.0 이상: 고도비만 (2단계)

## 6. 예외 처리
1. 입력값 검증 (정규식 필터링)
ㄴ 성별, 나이, 키, 체중은 숫자만 입력 가능 (문자 등 입력 시 무시)
ㄴ 성별, 나이, 키, 체중은 음수 입력 불가능
2. 계산 결과 검증
ㄴ 비만도(BMI)는 숫자만 출력

## 7. 스타일(디자인) 참고
- 프로젝트 공통 CSS (tailwind) 및 JIKO-calculator.md 디자인 패턴 준수 (카드형 UI, 다크모드 적용).
- 결과를 클립보드에 복사할 수 있는 기능 제공.
