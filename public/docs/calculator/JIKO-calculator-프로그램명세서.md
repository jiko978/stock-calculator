# JIKO-calculator-프로그램명세서

이 문서는 JIKO 계산기(`app/calculator`) 프로젝트의 주요 소스 코드 리스트와 각 파일별 역할, 기술적 특성을 설명합니다.

## 1. 개요
- **기술 스택**: Next.js 15 (App Router), React, TypeScript, Tailwind CSS
- **핵심 특징**: 
  - 서버 컴포넌트 기반의 고성능 SEO 최적화
  - 하이브리드 슬러그(종목명-종목코드) 기반 동적 라우팅
  - PWA 지원 및 카카오톡 공유 최적화

---

## 2. 공통 컴포넌트 (`app/calculator/components/`)

| 소스명 | 설명 | 주요 역할 | 비고 |
| :--- | :--- | :--- | :--- |
| `Header.tsx` | 상단 레이아웃 | 서비스 로고, 메뉴 내비게이션, 홈 이동 | |
| `Footer.tsx` | 하단 레이아웃 | 저작권 정보, 앱 설치 버튼 연동, 사이트 정적 링크 | |
| `NavBar.tsx` | 페이지 내비 바 | 뒤로가기, 페이지 제목, 공유 버튼(ShareSheet) 통합 | 필수 상단 UI |
| `ShareSheet.tsx` | 공유 레이어 | 카카오톡 공유 SDK 연동, 클립보드 복사, 브라우저 기본 공유 | `Kakao.Share` 활용 |
| `PWAInstallProvider.tsx` | PWA 상태 관리 | 브라우저 설치 권한(`beforeinstallprompt`) 감지 및 상태 제공 | Context API 사용 |
| `InstallBanner.tsx` | 설치 유도 배너 | 모바일 사용자 대상 PWA 설치 제안 | 슬라이드 애니메이션 |
| `RegisterSW.tsx` | 서비스 워커 등록 | PWA 구현을 위한 `sw.js` 등록 로직 | |

---

## 3. 주식 계산기 모듈 (`app/calculator/stock/`)

### 3.1 데이터 및 유틸리티
- **`data/stocks.json`**: 코스피/코스닥 전 종목 데이터 (약 2,500개). `name`, `code` 필드로 구성되어 슬러그 매핑 및 SEO 타이틀 생성에 사용됨.

### 3.2 수익률 계산기 (`profit-rate/`)
| 소스명 | 설명 | 주요 특징 |
| :--- | :--- | :--- |
| `page.tsx` | 범용 페이지 | 기본 수익률 계산기 진입로 |
| `[slug]/page.tsx` | 종목 전용 페이지 | 하이브리드 슬러그 분석, 맞춤형 SEO 메타데이터 및 JSON-LD 생성 |
| `ProfitRate.tsx` | 계산기 본체 | 수익률/수익금 연산, 결과 바 차트 시각화, 종목 배지 노출 |

### 3.3 평균단가 계산기 (`avg-price/`)
| 소스명 | 설명 | 주요 특징 |
| :--- | :--- | :--- |
| `page.tsx` | 범용 페이지 | 기본 평단가 계산기 진입로 |
| `[slug]/page.tsx` | 종목 전용 페이지 | 종목 매핑 및 전략적 SEO 키워드 노출 |
| `AvgPrice.tsx` | 계산기 본체 | 최대 10차 매수 리스트 관리, 물타기 계산 로직, 비중 시각화 |

---

## 4. 핵심 기술 로직

### 4.1 하이브리드 슬러그 파싱 (`findStock`)
`[slug]/page.tsx`에서 사용되는 핵심 함수로, URL 슬러그를 아래 3단계로 분석하여 종목을 매핑합니다.
1. **하이브리드**: `삼성전자-005930` (이름과 코드를 모두 분리 추출)
2. **코드**: `005930` (6자리 숫자인 경우 `stocks.json`에서 검색)
3. **이름**: `삼성전자` (`stocks.json`에서 검색하거나 직접 이름으로 사용)

### 4.2 SEO 메타데이터 패턴
사용자 제안에 따라 4가지 정보(종목명, 종목코드, 주요 기능, 브랜드)를 조합하여 외부 검색 엔진 노출을 극대화합니다.
- 예: `005930 삼성전자 수익률 계산기 | 삼성전자 투자 수익 계산 - JIKO`

---

## 5. 제안 및 향후 확장 계획
- **상태 관리 최적화**: 현재 로컬 `state` 기반이나, 계산 이력 저장을 위해 `localStorage` 연동 제안.
- **이미지 자동 생성**: 공유 시 종목별 수익 현황을 캔버스로 그려 커스텀 공유 이미지 제공 기능 제안.
- **실시간 시세 연동**: 현재 사용자 입력 기반이나, 외부 API 연동을 통한 실시간 현재가 반영 제안.
